HUGGINGFACE_API_KEY=
GROQ_API_KEY=

# Neo4j (AuraDB Free)
NEO4J_URI=
NEO4J_USER=
NEO4J_PASSWORD=

# Groq model
GROQ_MODEL=

# DeepSeek — dùng cho CV Analyzer (extraction + gap analysis)
DEEPSEEK_API_KEY=
DEEPSEEK_API_KEYS=key2,key3,key4    # comma-separated backup keys, round-robin
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# HuggingFace embedding
HF_EMBEDDING_MODEL=
HF_EMBEDDING_DIM=

# App
API_PORT=

GOOGLE_API_KEY=

# Chatbot stack (apps/)
GROQ_SUGGEST_MODEL=
EMBEDDING_MODEL=
EMBEDDING_DIM=
REDIS_URL=
AI_SERVICE_URL=
AI_SERVICE_TIMEOUT=
PORT=
JWT_SECRET=
THROTTLE_TTL=
THROTTLE_LIMIT=
# Leave empty to use nginx proxy (production). Set http://localhost:3000 for local dev.
VITE_API_URL=

# Gateway CORS — set to your domain or * for open access
CORS_ORIGIN=*